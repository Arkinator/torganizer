package torganizer.core.test;

public class FileDataSourceTest {
	// @Test
	// public void storeAndRestoreTest() {
	// final String playerName = "fjdksalfsd";
	// if (FileSystemDataSource.dataSourceFile.exists()) {
	// FileSystemDataSource.dataSourceFile.delete();
	// }
	//
	// FileSystemDataSource source = new FileSystemDataSource();
	// source.persistEntity(new Player(playerName));
	// source.commitChanges();
	//
	// source = new FileSystemDataSource();
	// assertEquals(playerName, source.findUserByName(playerName).getName());
	// }
}
